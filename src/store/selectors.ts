import {createSelector} from 'reselect';


// const LocationState = (state: any): any => state.app.locationState;

export const team_a_select = (state:any) => {
  return state
};

// export const team_a = createSelector(InvoiceState, invoice => invoice);
